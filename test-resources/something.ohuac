ns some_ns;

require ohua.math (add, isZero);

fn sqare (x) {
    add(x, x)
}

fn algo1 (someParam) {
    let a = square(someParam);

    let coll0 = for i in coll {
        square(i)
    };

    if (isZero(a)) {
        coll0
    } else {
        a
    }
    
}

fn main (param) {
    algo0(param)
}
